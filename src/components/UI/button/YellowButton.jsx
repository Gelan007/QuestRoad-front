import React from 'react';
import s from "./Buttons.module.css";

const YellowButton = (props) => {
    return (
        <button className={s.yellowBtn} {...props}>
            
        </button>
    );
};

export default YellowButton;